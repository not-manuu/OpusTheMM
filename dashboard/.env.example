# Frostbyte Dashboard - Environment Variables
#
# For LOCAL development: Copy to .env.local
# For VERCEL deployment: Set these in Vercel Project Settings > Environment Variables

# Backend API URL (your Render service URL)
# Local: http://localhost:3000
# Production: https://your-service-name.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:3000

# WebSocket URL (same as API URL but with ws/wss protocol)
# Local: ws://localhost:3000
# Production: wss://your-service-name.onrender.com
NEXT_PUBLIC_WS_URL=ws://localhost:3000

# API Key (must match API_KEY in backend .env)
NEXT_PUBLIC_API_KEY=your-api-key-here
